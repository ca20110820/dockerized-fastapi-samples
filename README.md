# Basic Samples of Dockerized FastAPI

- Build
    ```bash
    docker build -t dockerized-fastapi .
    ```
- Run
    ```bash
    docker run -t -p 8000:8000 dockerized-fastapi
    ```
- View
    `http://localhost:8000`